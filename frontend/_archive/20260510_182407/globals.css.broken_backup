@tailwind base;
@tailwind components;
@tailwind utilities;

html, body {
  background: radial-gradient(ellipse at top, #0E1F1A 0%, #0A1410 60%);
  color: #E8EDE8;
  font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
  min-height: 100vh;
}

* { box-sizing: border-box; }
